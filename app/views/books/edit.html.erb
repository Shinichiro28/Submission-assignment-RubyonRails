<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF=8">
  <link rel="stylesheet" href="/bookers/app/assets/stylesheets/application.css">
</head>

<body>
<h1>Editing Book</h1>
<!--エラーメッセージ-->
<%if@book.errors.any?%>
  <div class="index-error">
    <h2 class="index-error-title"><%=pluralize(@book.errors.count,"error")%>prohibited this product from being saved:</h2>
    <ul>
      <%@book.errors.full_messages.each do |message|%>
      <li class="index-error-text"><%=message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<!--投稿フォーム-->
<%= form_with model: @book,local: true do |f| %>
<div class="editing-book">
  <h4 class="editing-book-title">Title</h4>
  <%= f.text_field :title %>

  <h4  class="editing-book-title">Body</h4>
  <%= f.text_area :body %>

  <div class="update-book"><%= f.submit 'Update Book' %>
  </div>
<% end %>
<%=link_to "Show",book_path(@book)%>
 |
<%=link_to"Back",books_path%>
</div>
</body>
</html>